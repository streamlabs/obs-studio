# About

This project can be imported into another app and built there.

## Prerequisites

This project requires an Apple Developer certificate. The environment variable `APPLE_TEAM_ID` should be set to a valid TEAM ID that corresponds to the bundle identifier.

## How to Build

Run the `build-slobs-cameraextension.sh` script. It will run cmake to build the project on macOS 13.

